[project]
name = "git-ftp-docs"
version = "0"
description = "Sources of the git-ftp documentation site at https://git-ftp.github.io"
requires-python = ">=3.10"
dependencies = [
    "mkdocs-material>=9.5,<10",
]

[tool.uv]
package = false
